# Test Specification: `design-tokens.test.ts`

## Source File Under Test
`src/ui/src/styles/design-tokens.css`

## Test File
`src/ui/src/styles/design-tokens.test.ts`

---

## `DesignTokens`

### Happy Path — Token Definitions

| Test ID | Category | Description | Setup | Input | Expected |
|---|---|---|---|---|---|
| `defines_color_bg_page` | `unit` | Token `--color-bg-page` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--color-bg-page` in the parsed token map. | Value is `#F5F6F8`. |
| `defines_color_bg_surface` | `unit` | Token `--color-bg-surface` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--color-bg-surface` in the parsed token map. | Value is `#FFFFFF`. |
| `defines_color_bg_canvas_idle` | `unit` | Token `--color-bg-canvas-idle` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--color-bg-canvas-idle` in the parsed token map. | Value is `#FFFFFF`. |
| `defines_color_text_primary` | `unit` | Token `--color-text-primary` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--color-text-primary` in the parsed token map. | Value is `#2C3E50`. |
| `defines_color_text_muted` | `unit` | Token `--color-text-muted` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--color-text-muted` in the parsed token map. | Value is `#6B7B8D`. |
| `defines_color_border` | `unit` | Token `--color-border` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--color-border` in the parsed token map. | Value is `#D4DAE0`. |
| `defines_color_primary` | `unit` | Token `--color-primary` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--color-primary` in the parsed token map. | Value is `#5B8CB8`. |
| `defines_color_primary_disabled` | `unit` | Token `--color-primary-disabled` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--color-primary-disabled` in the parsed token map. | Value is `#A8C4DC`. |
| `defines_color_secondary` | `unit` | Token `--color-secondary` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--color-secondary` in the parsed token map. | Value is `#6B7B8D`. |
| `defines_color_secondary_disabled` | `unit` | Token `--color-secondary-disabled` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--color-secondary-disabled` in the parsed token map. | Value is `#D4DAE0`. |
| `defines_color_white` | `unit` | Token `--color-white` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--color-white` in the parsed token map. | Value is `#FFFFFF`. |
| `defines_font_family` | `unit` | Token `--font-family` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--font-family` in the parsed token map. | Value is `system-ui, -apple-system, sans-serif`. |
| `defines_font_size_heading` | `unit` | Token `--font-size-heading` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--font-size-heading` in the parsed token map. | Value is `1.5rem`. |
| `defines_font_size_body` | `unit` | Token `--font-size-body` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--font-size-body` in the parsed token map. | Value is `1.1rem`. |
| `defines_font_size_small` | `unit` | Token `--font-size-small` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--font-size-small` in the parsed token map. | Value is `0.85rem`. |
| `defines_font_size_caption` | `unit` | Token `--font-size-caption` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--font-size-caption` in the parsed token map. | Value is `0.8rem`. |
| `defines_font_size_canvas_label` | `unit` | Token `--font-size-canvas-label` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--font-size-canvas-label` in the parsed token map. | Value is `14px`. |
| `defines_spacing_xs` | `unit` | Token `--spacing-xs` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--spacing-xs` in the parsed token map. | Value is `4px`. |
| `defines_spacing_sm` | `unit` | Token `--spacing-sm` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--spacing-sm` in the parsed token map. | Value is `8px`. |
| `defines_spacing_md` | `unit` | Token `--spacing-md` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--spacing-md` in the parsed token map. | Value is `12px`. |
| `defines_spacing_lg` | `unit` | Token `--spacing-lg` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--spacing-lg` in the parsed token map. | Value is `16px`. |
| `defines_spacing_xl` | `unit` | Token `--spacing-xl` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--spacing-xl` in the parsed token map. | Value is `24px`. |
| `defines_radius_sm` | `unit` | Token `--radius-sm` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--radius-sm` in the parsed token map. | Value is `4px`. |
| `defines_radius_md` | `unit` | Token `--radius-md` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--radius-md` in the parsed token map. | Value is `8px`. |
| `defines_canvas_width` | `unit` | Token `--canvas-width` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--canvas-width` in the parsed token map. | Value is `800`. |
| `defines_canvas_height` | `unit` | Token `--canvas-height` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--canvas-height` in the parsed token map. | Value is `450`. |
| `defines_canvas_aspect` | `unit` | Token `--canvas-aspect` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--canvas-aspect` in the parsed token map. | Value is `16/9`. |
| `defines_dropdown_max_height` | `unit` | Token `--dropdown-max-height` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--dropdown-max-height` in the parsed token map. | Value is `300px`. |
| `defines_dropdown_list_max_height` | `unit` | Token `--dropdown-list-max-height` is defined on `:root`. | Read the design-tokens CSS file as raw text (via Vite `?raw` import). Parse the `:root` rule to extract custom property declarations into a key-value map. | Look up `--dropdown-list-max-height` in the parsed token map. | Value is `220px`. |
